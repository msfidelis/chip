version: "3.2"
services:
  chip:
    build:
        context: ./
        dockerfile: Dockerfile.dev
    environment:
      - ENVIRONMENT=dev
    ports:
        - 8080:8080
    volumes:
        - ./:/go/src/chip